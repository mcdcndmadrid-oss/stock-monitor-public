name: Smoke Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout public code
        uses: actions/checkout@v4

      - name: Check Python and workspace
        run: |
          python --version
          pip --version
          echo "Archivos en el workspace:"
          ls -la
